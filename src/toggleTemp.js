const temp = () => {
  const toggle = document.getElementById('togBtn');
  toggle.addEventListener('click', () => {
    // evt.preventDefault();
    // console.log('I was clicked!!!');

  });
};

export default temp;
